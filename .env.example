# GitHub personal access token — optional, but raises the API rate limit from 60/hr to 5000/hr
GITHUB_TOKEN=

# Postgres (defaults match docker-compose.yml if left unset)
DATABASE_USERNAME=gograbbit
DATABASE_PASSWORD=gograbbit
